#include <iostream>

// TUI frontend (same functionality as the GUI, for remote/lightweight use).
// Placeholder until ftxui is wired in.
int main() {
    std::cout << "omarchy-kids-control-tui (placeholder)\n";
    return 0;
}
